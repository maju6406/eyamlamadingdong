{
    "puppet_task_version": 1,
    "supports_noop": false,
    "description": "Automates the steps involved on installing and configuring eyaml on Puppet Enterprise",
    "parameters": {
        "paths": {
            "description": "Additional hiera search paths separated by commas. Default is just common.eyaml",
            "type": "Optional[String[1]]"
        },
        "configure_global_hiera": {
            "description": "Configure the global hiera. Defaults to false.",
            "type": "Optional[Boolean]"
        }
    }
}